name: openyc_flutter_sdk_example
description: Minimal example demonstrating openyc_flutter_sdk usage.
publish_to: 'none'  # this is an example, not published
version: 0.1.0+1

environment:
  sdk: ^3.5.0
  flutter: ">=3.16.0"

dependencies:
  dio: ^5.7.0
  flutter:
    sdk: flutter
  openyc_flutter_sdk:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
